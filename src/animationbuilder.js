export class AnimationBuilder {
  constructor (name, initialState) {
    this._name = name
    this._initialState = initialState
  }

  to (duration, nextState) {
  }

  buildCss () {
    return ''
  }
}
